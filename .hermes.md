# .hermes.md — Hermes Agent Repo Operations

This document defines everything Hermes Agent must follow when working in the **NeuroLift-Technologies/nlt-world-engine** repository. It consolidates governance rules, templates, repo-specific context, and operational procedures into a single reference.

---

## Mandatory Reading Order (Every Session)

1. `NLT-DEV-OTOI.md` — Repo-local coding agent contract (root level)
2. `AGENTS.md` — Internal gateway (root level)
3. `CLAUDE.md` — Project-specific context (root level)
4. `docs/active-threads.md` — Current work state
5. `.hermes.md` — This file (operational reference)

---

## Final Authority

**Joshua W. Dorsey, Sr.** is the final authority on all architectural, deployment, UX, and strategic decisions. Escalate. Do not guess.

---

## Commit Format

Every commit must follow:

```
[AGENT_NAME] type(scope): description
```

- **AGENT_NAME:** `HERMES`
- **Allowed types:** `feat`, `fix`, `docs`, `refactor`, `chore`, `test`, `ci`
- **Scope:** Short noun phrase for the area affected
- **Description:** Imperative mood, no trailing period

**Examples:**
```
[HERMES] feat(plugin): add NLTGovernanceSubsystem WorldSubsystem base class
[HERMES] fix(build): correct Build.cs include paths for ASFDK-C++ pillars
[HERMES] docs(governance): write handoff record for Phase 7 UE integration
[HERMES] fix(phase7): resolve __isoc23_strtoull linker errors on glibc < 2.38
```

**Template:** `templates/commit-message.md`

---

## Pull Request Workflow (Non-Negotiable)

- **Always work from a feature branch** — never commit directly to `main`
- **Always open a Pull Request** — never push directly to `main` or protected branches
- **Use the NLT PR template:** `PULL_REQUEST_TEMPLATE/agent-contribution.md`
- **PR description must include:** Agent Contribution Checklist, scope declaration, changes summary, build verification, governance/handoff notes
- **PR reviewers on this repo:** CodeRabbit (automated), Poolside AI, OpenCode, kilo-code-bot, human approvers (JDUB1216)
- **Attribute review comments to the correct source** — don't guess who said what

---

## Guardrails (Non-Negotiable)

| Guardrail | Details |
|---|---|
| No LLM provider lock-in | Do not hardcode or commit to a specific LLM provider |
| No architecture decisions | Database, deployment, framework choices require human sign-off |
| No production deployments | Human must explicitly approve all production actions |
| No credential storage | Never store secrets, tokens, or credentials in code or VCS |
| No external integrations | Third-party service connections require Joshua's approval |
| PR-only workflow | Feature branches + Pull Requests only; never push to `main` |
| No OTOI self-amendment | `NLT-DEV-OTOI.md` cannot be changed by agents |

---

## Escalation Protocol

**Escalate to Joshua immediately when:**
- Task scope is unclear or conflicts with existing work
- An architectural or deployment decision is required
- A blocker cannot be resolved by the agent
- An ethical concern arises
- LLM provider or external service selection is needed

**Escalation format:** `templates/escalation.md`

**Escalation file location:** `docs/escalations/[date]-[topic].md`

**GitHub issue template:** `ISSUE_TEMPLATE/agent-escalation.md`

---

## Handoff Protocol

At the end of every significant session:

1. Update `docs/active-threads.md` in the working repo
2. Write a handoff record to `docs/agent-log/handoffs/` using `templates/handoff-record.json`
3. Document any open escalations in `docs/escalations/`
4. Summarize decisions made and decisions pending

**Handoff record location:** `docs/agent-log/handoffs/[date]-[session-id].json`

**Handoff template:** `templates/handoff-record.json`

---

## Active Thread Management

- Read `docs/active-threads.md` at session start
- Update it during the session as threads are opened or resolved
- Leave it accurate at session end

**Thread entry format:**
```markdown
### Thread: [Thread ID]
**Status:** [open | blocked | resolved]
**Owner:** [Agent or human responsible]
**Started:** [ISO 8601 timestamp with offset]
**Last updated:** [ISO 8601 timestamp with offset]
**Summary:** [One-paragraph description]
**Blockers:** [Any blocking conditions]
**Next action:** [Specific next step required]
```

---

## Intent Logging

For significant decisions, log intent before acting using `templates/intent-log.md`:

```markdown
## Intent Log Entry

**Date:** [ISO 8601]
**Agent:** Hermes
**Action:** [What you intend to do]
**Rationale:** [Why this is the right action]
**Risks:** [Potential downsides or unknowns]
**Alternatives considered:** [Other approaches]
**Escalation needed:** [yes | no]
```

---

## Repo-Specific Context

### Domain
- **Project:** WorldEngine — UE5.8-based AI training simulation environment
- **Engine:** Unreal Engine 5.8 (EngineAssociation "5.8" in .uproject)
- **Engine install:** `/home/joshd/Documents/NLT/Engine/`
- **Project file:** `WorldEngine/WorldEngine.uproject`

### Architecture
- **Base module:** `WorldEngine` (Runtime, Default loading phase)
- **Plugin:** `NLTGovernanceSubsystem` — UE WorldSubsystem integrating ASFDK-C++ governance
- **Plugin location:** `WorldEngine/Plugins/NLTGovernanceSubsystem/`
- **ASFDK-C++ dependency:** Symlinked at `WorldEngine/Plugins/NLTGovernanceSubsystem/ThirdParty/ASFDK` → `/home/joshd/Desktop/nlt-repos/asfdk-cplus`
- **Build system:** UBT (Unreal Build Tool) — NOT cmake
- **Target name:** `WorldEngineEditor` (matching `WorldEngineEditor.Target.cs`)
- **UBT path:** `/home/joshd/Documents/NLT/Engine/Build/BatchFiles/Linux/Build.sh`
- **GenerateProjectFiles:** `/home/joshd/Documents/NLT/Engine/Build/BatchFiles/Linux/GenerateProjectFiles.sh`

### Build Commands
```bash
# Regenerate project files
bash /home/joshd/Documents/NLT/Engine/Build/BatchFiles/Linux/GenerateProjectFiles.sh \
  -project=/home/joshd/Desktop/nlt-repos/nlt-world-engine/WorldEngine/WorldEngine.uproject \
  -game -engine

# Build editor (CORRECT target name is WorldEngineEditor, NOT WorldEditor)
bash /home/joshd/Documents/NLT/Engine/Build/BatchFiles/Linux/Build.sh \
  WorldEngineEditor Linux Development \
  /home/joshd/Desktop/nlt-repos/nlt-world-engine/WorldEngine/WorldEngine.uproject -verbose

# Launch editor
/home/joshd/Documents/NLT/Engine/Binaries/Linux/UnrealEditor \
  /home/joshd/Desktop/nlt-repos/nlt-world-engine/WorldEngine/WorldEngine.uproject -log
```

### Plugin Structure
```
WorldEngine/Plugins/NLTGovernanceSubsystem/
├── NLTGovernanceSubsystem.uplugin    ← Module descriptor (Name, Type, LoadingPhase, Plugins deps)
├── Source/NLTGovernanceSubsystem/
│   ├── NLTGovernanceSubsystem.Build.cs  ← Module rules (deps, include paths, lib linking)
│   ├── Public/
│   │   └── NLTGovernanceSubsystem.h    ← UNLTGovernanceSubsystem (UWorldSubsystem) + module interface
│   └── Private/
│       ├── NLTGovernanceSubsystem.cpp  ← Subsystem implementation (BlueprintCallable getters)
│       ├── AgentGovernanceState.h/.cpp ← FAgentGovernanceState struct
│       └── NLTGovernanceModule.cpp     ← IMPLEMENT_MODULE(FNLTGovernanceSubsystemModule, NLTGovernanceSubsystem)
└── ThirdParty/ASFDK → /home/joshd/Desktop/nlt-repos/asfdk-cplus  (symlink)
```

### Build.cs Configuration
- **CppStandard:** Cpp23
- **PCHUsage:** UseExplicitOrSharedPCHs
- **PublicDependencyModuleNames:** Core, CoreUObject, Engine, LearningAgents, Json, JsonUtilities, GameplayTasks
- **PrivateDependencyModuleNames:** Projects, NavigationSystem, AIModule, MassEntity, MassCore, MassSignals, MassEngine, MassCommon, MassSimulation, Sockets, Networking
- **Include paths:** `ThirdParty/ASFDK/packages/{include,asfdk/include,toi/include,otoi/include,rrt-advocate/include,sleepwalker/include}`
- **Library:** `ThirdParty/ASFDK/lib/libasfdk.a` (pre-built static library)

### ASFDK-C++ Pillar Layout
```
asfdk-cplus/packages/
├── include/           ← Transitive deps (nlohmann/json, tl/expected, spdlog)
├── asfdk/             ← Umbrella header (ASFDK.h, ASFDK.cpp, GLIBC23Compat.cpp)
├── toi/               ← TermsOfInteraction pillar
├── otoi/              ← OTOIManager pillar
├── rrt-advocate/      ← RRTAdvocate pillar
└── sleepwalker/       ← SleepwalkerProtocol pillar
```

### Key Rules
1. **Never list individual .cpp/.h files in Build.cs** — UBT auto-discovers Source/*.cpp
2. **Module name must match exactly:** .uplugin "Name" == Source/ folder == Build.cs class == IMPLEMENT_MODULE second arg
3. **Build.cs requires `using System.IO;`** for Path/Directory helpers
4. **Target name is `WorldEngineEditor`** — using `WorldEditor` causes RulesError
5. **Symlink must be at plugin root** (`Plugins/NLTGovernanceSubsystem/ThirdParty/ASFDK`) — not inside Source/
6. **libasfdk.a must be built separately** before UBT build — see asfdk-cplus README
7. **Deep clean when changing plugin structure:** rm -rf Binaries/Intermediate/DerivedDataCache, regenerate project files

### Deep Clean Procedure
```bash
cd /home/joshd/Desktop/nlt-repos/nlt-world-engine/WorldEngine
rm -rf Binaries Intermediate DerivedDataCache
rm -f WorldEngine.sln
# Then regenerate project files (see Build Commands above)
```

---

## Templates Reference

| Template | Location | Use When |
|---|---|---|
| Agent Registration | `templates/agent-registration.json` | Session start — self-register |
| Handoff Record | `templates/handoff-record.json` | End of significant session |
| Escalation | `templates/escalation.md` | Escalating to Joshua |
| Intent Log | `templates/intent-log.md` | Before significant decisions |
| Commit Message | `templates/commit-message.md` | Every commit |
| PR Checklist | `PULL_REQUEST_TEMPLATE/agent-contribution.md` | Every pull request |
| Governance Proposal | `ISSUE_TEMPLATE/governance-proposal.md` | Amending OTOI |
| Agent Escalation Issue | `ISSUE_TEMPLATE/agent-escalation.md` | Filing escalation on GitHub |

---

## File Map

```
NLT-DEV-OTOI.md                        ← Repo-local agent contract
AGENTS.md                               ← Internal gateway
CLAUDE.md                               ← Project context
.hermes.md                              ← This file (Hermes operations)
nltotoi.json                            ← Discovery manifest

.nltotoi/
├── README.md                           ← Namespace overview
├── index/governance-files.md          ← File registry
├── contracts/README.md                ← Contract namespace
├── proposals/validation-roadmap.md    ← Validation roadmap
└── scripts/validate-governance.sh     ← Governance validation

templates/
├── agent-registration.json            ← OTOI Section 3 registration format
├── handoff-record.json                ← OTOI Section 5 handoff format
├── escalation.md                      ← OTOI Section 4.3 escalation format
├── intent-log.md                      ← Intent logging template
└── commit-message.md                  ← Commit format reference

ISSUE_TEMPLATE/
├── agent-escalation.md                ← GitHub escalation issue form
└── governance-proposal.md             ← OTOI amendment proposal form

PULL_REQUEST_TEMPLATE/
└── agent-contribution.md              ← Agent PR checklist

workflows/
└── validate-governance.yml            ← CI: runs validate-governance.sh

SOPs/
├── new-agent-onboarding.md            ← How to onboard a new coding agent
├── repo-governance-setup.md           ← How to add governance to a new NLT repo
└── incident-response.md               ← What to do when an agent goes off-rails

WorldEngine/
├── WorldEngine.uproject               ← Project descriptor (EngineAssociation 5.8)
├── Source/WorldEngine/                ← Base game module
│   ├── WorldEngine.Build.cs
│   ├── WorldEngine.Target.cs
│   └── WorldEngineEditor.Target.cs    ← Editor target (Type=Editor, ExtraModuleNames=["WorldEngine"])
├── Plugins/NLTGovernanceSubsystem/    ← Governance plugin (see Plugin Structure above)
├── Binaries/                          ← Compiled .so files (cleanable)
├── Intermediate/                      ← Build artifacts (cleanable)
└── Saved/                             ← Logs, config, DDC

docs/
├── active-threads.md                  ← Current work state (read every session)
├── agent-log/
│   ├── handoffs/                      ← Handoff records
│   ├── registrations/                 ← Agent registrations
│   └── escalations/                   ← Escalation records
└── context/                           ← Project context docs
```

---

## Common Mistakes to Avoid

1. **Using `WorldEditor` instead of `WorldEngineEditor`** — causes RulesError
2. **Pushing directly to main** — always use a feature branch + PR
3. **Forgetting to update active-threads.md** — do it at session start AND end
4. **Writing handoff records with stale file lists** — verify `files_modified` matches actual diff
5. **Using wrong PR template** — always use `PULL_REQUEST_TEMPLATE/agent-contribution.md`
6. **Misattributing review comments** — Poolside AI, OpenCode, kilo-code-bot, CodeRabbit, and JDUB1216 all review PRs
7. **Placing ThirdParty symlink inside Source/** — must be at plugin root
8. **Missing `using System.IO;` in Build.cs** — causes CS0103 generation error
9. **Including LearningAgentsTraining in .uplugin** — plugin not installed in engine
10. **Skipping handoff records** — every significant session needs one
11. **Not escalating** — when in doubt, escalate. It's not failure; it's correct protocol.
12. **Forgetting to rebuild libasfdk.a** after ASFDK-C++ changes — stale .so will link old code

---

## Quick Reference

| Action | Protocol |
|---|---|
| Session start | Read OTOI → AGENTS.md → CLAUDE.md → active-threads.md → .hermes.md → register → feature branch |
| Commit format | `[HERMES] type(scope): description` |
| Escalation trigger | Scope unclear, arch decision needed, blocker, ethical concern |
| Escalation target | Joshua W. Dorsey, Sr. |
| Handoff required | End of every significant session |
| Amendment process | Governance proposal → Joshua review → version bump |
| Build verification | `Build.sh WorldEngineEditor Linux Development` → Result: Succeeded |
| PR creation | Use `PULL_REQUEST_TEMPLATE/agent-contribution.md` |
| Deep clean | rm -rf Binaries/Intermediate/DerivedDataCache + regenerate project files |

---

*ORG-DEV-OTOI-1.0.3 | NeuroLift Technologies | Governed by Solidarity Framework & HAIEF*
